apiVersion: v1
kind: Service

metadata:
  name: knowledgeops-frontend
  namespace: knowledgeops

spec:

  selector:
    app: knowledgeops-frontend

  ports:

  - port: 80
    targetPort: 80

  type: ClusterIP